import { DOMSelectors } from "./DOM";
import { quizGame, endings } from "./questions";

//accept input of answer choice, take answerIndexValue of input and find the object within quizGame whose eventIndexValue === answerIndexValue of input, display, go to step one for next question

$("#next").onclick(function () {
  alert("I'm not gonna curse");
});
